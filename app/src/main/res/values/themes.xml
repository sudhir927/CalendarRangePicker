<?xml version="1.0" encoding="utf-8"?>
    <resources>
        <!-- Base application theme. -->
        <style name="Base.Theme.CalendarRange" parent="Theme.AppCompat.DayNight">
            <!-- Customize your theme here. -->
            <item name="colorPrimary">@color/calendar_selected</item>
            <item name="colorPrimaryDark">@color/calendar_range</item>
            <item name="colorAccent">@color/calendar_today</item>
        </style>

        <style name="Theme.CalendarRange" parent="Base.Theme.CalendarRange" />
    </resources>
